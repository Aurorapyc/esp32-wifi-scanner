# 使用你的代理安装 esp32 板包

你有本地代理（`127.0.0.1:10808`），我们可以让 Arduino IDE 的下载直接走这个代理，
这样就不需要再用 GitHub 镜像了。

## 操作步骤（推荐）

1. **关闭 Arduino IDE**（必须完全关闭）
2. 打开本目录，**双击 `set_proxy.bat`**
   - 脚本会先测试你的 `127.0.0.1:10808` 代理：
     - 自动识别它是 HTTP 代理还是 SOCKS5 代理
     - 测试 GitHub 是否可以通过代理访问
   - 然后把代理写进 Arduino 的配置文件 `arduino-cli.yaml`
   - 如果代理端口不是 10808，用记事本打开 `set_arduino_proxy.ps1`，
     把开头 `$port = 10808` 改成你的端口再运行
3. 看到 `Done!` 后按回车退出
4. **重新打开 Arduino IDE** → 开发板管理器 → 安装 **esp32 by Espressif Systems**
   - 工具 → 开发板 → 开发板管理器 → 搜索 `esp32` → 安装

## 如果不想用脚本（手动在 IDE 里配代理）

1. `文件 → 首选项 → 网络`（Network）
2. 勾选「手动配置代理」
3. 主机填 `127.0.0.1`，端口填 `10808`
4. 确定后重新安装

> 注意：如果 `10808` 是 SOCKS5 代理，IDE 图形界面里可能只支持 HTTP 代理，
> 这时请用脚本方式（脚本会自动写成 `socks5://` 格式）。

## 之前用过镜像修复脚本怎么办？

如果你之前运行过 `run_fix.bat`（把下载地址改成了 GitHub 镜像），
现在有代理了，**两种方式都能用**，不冲突：
- 走镜像：直接用，代理不影响
- 想改回直连 GitHub（走你的代理）：把 Arduino15 目录里
  `package_esp32_index.json.bak_xxx` 备份文件复制回去覆盖原文件即可

## 验证代理是否生效

重新安装 esp32 时，如果串口监视器 / 日志里显示下载进度且不超时，
说明代理生效了。装完后在 `工具 → 开发板 → ESP32 Arduino` 里能看到
**ESP32 Dev Module**，选它 + 对应 COM 口，就能编译烧录 WiFi 扫描仪了。

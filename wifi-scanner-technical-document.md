# ESP32 WiFi 扫描仪技术文档

> 本文档遵循《中文技术文档的写作规范》编写，规范来源为 [ruanyf/document-style-guide](https://github.com/ruanyf/document-style-guide)。

## 简介

ESP32 WiFi 扫描仪是一款基于 ESP32 微控制器的网络诊断工具。它可以扫描周围所有 2.4 GHz 频段的 Wi-Fi 信号，并通过内置网页实时展示每个网络的 SSID（服务集标识）、信号强度、信道、BSSID（基本服务集标识）和加密方式。

本项目以较低成本实现了无线网络勘查工具的核心功能，适合用于家庭网络优化、无线信号覆盖检查，以及学习嵌入式网络开发。

### 主要功能

- 扫描周围所有 2.4 GHz 频段的 Wi-Fi 网络，并按信号强度从强到弱排序。
- 内置网页实时展示扫描结果，每 4 秒自动刷新。
- 每条记录展示 SSID、信道、BSSID、RSSI（接收信号强度指示）和加密类型。
- 信号强度以可视化强度条和颜色分级显示，分为极强、强、一般、弱、很弱五个等级。
- 支持识别不广播 SSID 的隐藏网络，显示为“〈隐藏网络〉”。
- 支持按 SSID 关键字搜索过滤，并提供手动重新扫描按钮。

### 技术规格

| 项目 | 规格 |
| --- | --- |
| 主控芯片 | ESP32（双核 Xtensa LX6，240 MHz） |
| 无线频段 | 2.4 GHz，不支持 5 GHz |
| 扫描间隔 | 4 秒，可配置 |
| 最大记录数 | 64 条 |
| 工作模式 | 热点模式（AP）或路由器模式（STA） |
| 开发环境 | Arduino IDE 与 arduino-esp32 开发板包 |
| 代码依赖 | 仅依赖 ESP32 官方核心自带的 Wi-Fi 库与 WebServer 库 |

## 快速上手

1. 用 USB 数据线将 ESP32 开发板连接电脑。
2. 在 Arduino IDE 中打开 `wifi_scanner/wifi_scanner.ino`。
3. 选择开发板“ESP32 Dev Module”和对应串口，点击上传。
4. 烧录完成后，打开串口监视器，波特率设置为 115200。
5. 根据串口提示，连接热点或访问路由器分配的地址，即可打开扫描页面。

## 硬件准备

| 硬件 | 说明 |
| --- | --- |
| ESP32 开发板 | 标准版即可，串口芯片为 CP2102 |
| USB 数据线 | 必须支持数据传输 |

本项目不需要任何额外接线。如果电脑无法识别串口，需要安装 CP210x 驱动。

## 软件环境

### Arduino IDE

从官网下载并安装 Arduino IDE 2.x。

### 开发板包

在 Arduino IDE 中，将以下网址添加到附加开发板管理器：

```
https://espressif.github.io/arduino-esp32/package_esp32_index.json
```

然后在开发板管理器中搜索并安装 esp32 开发板包。

### 网络受限时的处理

开发板包需要从 GitHub 下载，网络受限时可能安装失败。此时可以配置本地代理或使用 GitHub 加速镜像。本工作区提供了辅助脚本，使用方法见 `README.md`。

## 安装与烧录

1. 打开 `wifi_scanner/wifi_scanner.ino`。
2. 在“工具 → 开发板”中选择“ESP32 Dev Module”。
3. 在“工具 → 端口”中选择对应串口。
4. 点击上传按钮，等待编译和烧录完成。

如果上传过程卡在“Connecting...”状态，按住开发板上的 BOOT 按钮，出现提示时松开。

## 配置

固件开头的“配置区”集中了所有可配置项，位于 `wifi_scanner/wifi_scanner.ino` 文件顶部。

| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| `AP_SSID` | `WiFi-Scanner` | 热点名称 |
| `AP_PASS` | `12345678` | 热点密码，至少 8 位，留空表示开放热点 |
| `STA_SSID` | 空 | 路由器名称，留空使用热点模式 |
| `STA_PASS` | 空 | 路由器密码 |
| `SCAN_INTERVAL_MS` | `4000` | 扫描间隔，单位毫秒 |

### 热点模式

`STA_SSID` 留空时，ESP32 创建名为 `WiFi-Scanner` 的热点。手机或电脑连接该热点后，访问 `http://192.168.4.1` 即可打开页面。

### 路由器模式

填写 `STA_SSID` 和 `STA_PASS` 后，ESP32 会连接家庭路由器并自动获取局域网地址。该地址打印在串口监视器中，也可以在路由器管理页面的设备列表中查询。

### 连接失败时的回退机制

如果填写了路由器信息但连接失败，固件会在 15 秒后自动切换回热点模式。此时仍然可以连接 `WiFi-Scanner` 热点并访问 `http://192.168.4.1`。

注意，开机后的 15 秒内网页服务器尚未启动，页面暂时无法访问。

## 系统架构

### 软件流程

固件采用异步扫描方式，主循环交替处理网页请求和扫描结果。扫描流程如下：

1. 主循环每 4 秒发起一次异步扫描。
2. 扫描完成后，结果存入内存数组，并按信号强度排序。
3. 网页通过 JSON 接口读取最新扫描结果。
4. 浏览器每隔 4 秒请求一次接口并刷新表格。

这种设计保证了网页在扫描期间始终可以响应。

### 网页接口

固件内置 HTTP 服务器，提供以下接口：

| 路径 | 说明 |
| --- | --- |
| `/` | 扫描页面 |
| `/api/networks` | 最新扫描结果，JSON 格式 |
| `/scan` | 手动触发一次扫描 |

JSON 返回示例：

```json
{
  "count": 12,
  "scanning": 0,
  "networks": [
    {
      "ssid": "MyWiFi",
      "bssid": "AA:BB:CC:DD:EE:FF",
      "rssi": -45,
      "channel": 6,
      "enc": "WPA2"
    }
  ]
}
```

### 信号强度分级

信号强度根据 RSSI 值划分为五个等级，并使用颜色和强度条直观展示。

| 等级 | RSSI 范围（dBm） |
| --- | --- |
| 极强 | 大于等于 -50 |
| 强 | -60 至 -50 |
| 一般 | -70 至 -60 |
| 弱 | -80 至 -70 |
| 很弱 | 小于 -80 |

## 使用方法

### 打开扫描页面

连接热点或路由器后，用浏览器访问对应的 IP 地址。

### 页面功能

- 顶部卡片显示检测到的网络数量、显示数量和最后更新时间。
- 表格按信号强度排序，展示信号、SSID、信道、BSSID、强度和加密方式。
- 搜索框可以按 SSID 关键字过滤。
- “重新扫描”按钮可以手动触发扫描。
- 页面每 4 秒自动刷新。

### 查看运行状态

打开串口监视器，波特率设置为 115200，可以看到固件的启动信息、运行日志和当前工作模式。

## 常见问题

### 上传固件时提示连接失败

按住开发板上的 BOOT 按钮，点击上传，出现“Connecting...”提示时松开按钮。如果仍然失败，请更换 USB 数据线。

### 电脑无法识别串口

安装 CP210x 驱动后，重新插拔 USB 线。

### 页面无法打开

确认设备连接了正确的热点，并且访问的 IP 正确。部分手机连接热点时会提示“无互联网连接”，选择保持连接即可。

### 扫描列表为空

等待页面自动刷新两次。如果周围没有 2.4 GHz 网络，扫描结果为空。ESP32 不支持 5 GHz 频段。

### 连接了路由器但找不到 IP

在串口监视器中查看启动日志。如果显示“连接路由器失败，改用热点模式”，说明连接失败，此时需要访问 `http://192.168.4.1`。

## 完整项目需要的东西

当前项目可以正常运行，但作为一个完整、可维护、可分享的项目，还需要补充以下内容。

### 版本管理

- 建立 Git 仓库，管理固件和文档的版本历史。
- 添加 `.gitignore` 文件，忽略编译中间文件、驱动文件和 IDE 配置。
- 将代码托管到 GitHub 或 Gitee。

### 开源许可证

- 选择并添加 `LICENSE` 文件，例如 MIT 许可证，明确他人使用、修改和分发的权利。

### 文档体系

- 完善故障排查文档（Troubleshooting）。
- 建立术语表（Glossary），统一术语表达。
- 维护版本记录（ChangeLog），记录每次发布的变更。

### 自动化与测试

- 使用 GitHub Actions 在每次提交时自动编译校验固件。
- 为固件的核心逻辑编写单元测试，例如信号分级和 JSON 生成函数。
- 建立手动测试清单，覆盖热点模式、路由器模式和断网回退等场景。

### 可选增强功能

- 添加 mDNS 支持，通过 `http://esp32.local` 访问页面，无需查询 IP。
- 增加信道拥挤度分析，推荐最佳信道。
- 增加信号历史曲线，观察信号波动。
- 增加扫描结果导出，支持 CSV 格式。
- 增加 OTA 固件升级能力，方便远程更新。
- 增加 NTP 时间同步，让页面显示真实时间。

### 硬件完善

- 设计 3D 打印外壳，提高便携性。
- 考虑外接电源供电，脱离电脑独立运行。
- 优化天线位置，提高扫描灵敏度。

## 附录

### 项目结构

```
esp32/
├── wifi_scanner/
│   └── wifi_scanner.ino                    # 固件源码
├── README.md                               # 使用说明
├── wifi-scanner-technical-document.md      # 本文档
├── CHANGELOG.md                            # 版本记录
├── .gitignore                              # Git 忽略规则
├── .drivers/                               # CP210x 串口驱动
└── 网络辅助脚本
    ├── set_arduino_proxy.ps1               # 配置 Arduino 代理
    ├── fix_esp32_download.ps1              # 修复下载，镜像方案
    ├── fix_eof_download.ps1                # 修复下载中断
    └── download_esp32_with_resume.ps1      # 断点续传下载工具链
```

### 版本记录

版本记录见 `CHANGELOG.md`。

### 参考

- 本文档遵循《中文技术文档的写作规范》编写，来源为 [ruanyf/document-style-guide](https://github.com/ruanyf/document-style-guide)。
- ESP32 Arduino 开发资料为 [arduino-esp32](https://github.com/espressif/arduino-esp32)。

### 反馈

欢迎通过项目仓库提交问题或改进建议。

# 修复「Download failed: unexpected EOF」错误

## 这是什么错？

`unexpected EOF` 意思是**下载到一半连接被掐断了**（文件没下完）。
它和「连接超时」不同——超时是完全连不上，EOF 是连上了但中途断掉。

常见原因：

1. **缓存里有个残缺文件**：上次下载断掉后留下的半成品，
   Arduino 每次都拿这个坏文件再用，所以怎么装都失败
2. **代理（或镜像）把大文件下断了**：esp32 工具链一个文件就好几百 MB，
   本地代理对超大文件/长连接经常中途断流（你现在的 HTTP 代理 10808 就是这个情况）
3. 杀毒软件 / 防火墙拦截了大文件下载

## 最有效的修复：用「断点续传」下载工具链（推荐）

Arduino 自己的安装器遇到一次网络抖动就放弃、抛 EOF，然后从头再来。
**换个思路：用 curl（带断点续传 + 自动重试）替你把所有文件先下好，
放进 Arduino 的缓存目录，再让 Arduino 只解压、不再下载。**

工作区里的脚本 `download_esp32_with_resume.bat` 就是干这个的：

1. **保持代理开启**，关闭 Arduino IDE
2. **双击 `download_esp32_with_resume.bat`**
   - 脚本会自动读取你配好的代理（`arduino-cli.yaml` 里的，默认 `http://127.0.0.1:10808`）
   - 用 curl 下载 esp32 3.3.11 需要的所有文件（平台包 + 全部工具链，共几个 GB）
   - **断了会自动从断点继续**，不会从头开始；下完会校验 SHA-256
3. 等它显示 `All files downloaded & verified`（可能要一段时间，别关窗口）
4. **重新打开 Arduino IDE → 开发板管理器 → 安装 esp32**
   - Arduino 发现文件已经完整、校验通过，就直接解压，秒装完成

中途如果断了、或者你手动关了窗口，**再跑一次**即可，已下好的部分会跳过。

## 备选：清缓存 + 换下载路径

如果上面的续传脚本暂时不想用，也可以先试这个：

**双击 `run_fix_eof.bat`** —— 它会清掉 Arduino 的坏缓存、检测你当前走的是
代理还是镜像、测试哪条路现在通，并给出建议。

| 情况 | 怎么办 |
|------|--------|
| 用**代理**装的，一直 EOF | 代理下不动大文件。关闭 IDE 里的代理，运行 `run_fix.bat` 改用 GitHub 镜像（ghfast.top 优先）直连重装 |
| 用**镜像**装的，一直 EOF | 镜像截断了。改走代理：运行 `set_proxy.bat`，并恢复索引为直连 github.com（见下） |
| 都试了还是不行 | 关掉杀毒软件的文件监控再试；换网络环境（如手机热点）试 |

## 提示

- **续传脚本是最省心的**：它不怕断，断了继续，校验完整后 Arduino 秒装
- 安装/下载全程保持代理开启，中间别关
- 如果之前先跑了镜像脚本、现在想走代理：
  把 `C:\Users\Luna\AppData\Local\Arduino15` 下
  `package_esp32_index.json.bak_xxx`（脚本备份）复制回去覆盖
  `package_esp32_index.json`，让下载地址恢复为 github.com 直连
- 装完后在 `工具 → 开发板 → ESP32 Arduino` 里选 **ESP32 Dev Module** + 对应 COM 口，
  就能编译烧录我们的 WiFi 扫描仪了
